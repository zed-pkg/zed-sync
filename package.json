{
  "name": "@zed-pkg/sync",
  "version": "0.1.0",
  "description": "Offline-first sync: optimistic IndexedDB writes with a background outbox to Supabase/Postgres",
  "license": "MIT",
  "repository": "github:zed-pkg/zed-sync",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p .",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
