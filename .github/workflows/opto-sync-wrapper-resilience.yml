name: opto-sync wrapper resilience

on:
  pull_request:
    paths: ['.zpkg.toml', '.zpkg.lock', 'opto-sync-adapter.json', '.github/workflows/opto-sync-wrapper-resilience.yml']
  workflow_dispatch:

permissions:
  contents: read

jobs:
  wrapper-resilience:
    uses: opto-sync/opto-sync-e2e/.github/workflows/downstream-wrapper-reusable.yml@469b392d309413d7b763f2b9b01ea5e045fa0d80
